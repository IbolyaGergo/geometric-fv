\pagestyle{headings}
\linespread{1.3}
\usepackage[top=2.5cm, bottom=2.5cm, left=2.5cm, right=2.5cm, bindingoffset=1cm]{geometry}
\usepackage[slovak,english]{babel}

% Images and sub-figures
\usepackage{graphicx}
\usepackage{subcaption} 
\usepackage[font=small,margin=0.5cm,labelfont=bf]{caption} % Bold "Figure 1"

\setcounter{secnumdepth}{3} % Numbering up to \subsubsection
\setcounter{tocdepth}{2}    % Table of Contents shows up to \subsection

% Mathematical expressions
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amsthm} % Allows \newtheorem{theorem}{Theorem}

% Typography improvements (makes text blocks look smoother)
% The microtype package tries to "patch" (modify) standard LaTeX
% commands to make the typography perfect. It tries to patch the
% \item command (used in bullet points and numbered lists).
% However, babel (when loaded with Slovak) also modifies the
% \item command to handle specific Slovak enumeration standards.
\usepackage[nopatch=item]{microtype}
 
\usepackage{babel,csquotes,xpatch}
\usepackage[
	backend=biber,
    style=numeric,
	maxbibnames=99]{biblatex}
\addbibresource{bib/references.bib}

\usepackage{standalone}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{patterns}

% Hyperlinks
% Overleaf: "...usually, it has to be the last package to be imported."
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue
}

\usepackage{xurl}
% Load this AFTER \usepackage{biblatex} and \usepackage{hyperref}

\newcommand{\diff}{\ensuremath{\operatorname{d}\!}}
